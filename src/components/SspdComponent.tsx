import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SspdComponent = () => {
  return (
    <View>
      <Text>SspdComponent</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SspdComponent;
